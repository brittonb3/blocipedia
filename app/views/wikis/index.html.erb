<div class="container">
  <div class="col-md-4">
  <h1>Wikis <%= link_to "Create New Wiki", new_wiki_path(@wiki), class: 'btn btn-success' %></h1>
 <% @wikis.each do |wiki| %>
   <p><%= link_to markdown(wiki.title), wiki %></p>
 <% end %>
 </div>
</div>
