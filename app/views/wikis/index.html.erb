<h1>Wikis</h1>
<div class="col-md-4">
  <%= link_to "New", new_wiki_path(@wiki), class: 'btn btn-success' %>
</div>
 <% @wikis.each do |wiki| %>
   <p><%= link_to wiki.title, wiki %></p>
 <% end %>
 
